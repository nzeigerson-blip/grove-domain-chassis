apiVersion: v1
kind: Service
metadata:
  name: grove-domain
  namespace: grove
  labels:
    app: grove-domain
spec:
  type: ClusterIP
  selector:
    app: grove-domain
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
